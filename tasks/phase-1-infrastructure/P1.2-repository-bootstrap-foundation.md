# Task P1.2: Repository Bootstrap Foundation

- **Phase:** 1 - Infrastructure
- **Status:** Not Started
- **Priority:** P0
- **Owner Role:** infrastructure
- **Depends On:** P1.1
- **Parallel Safe With:** None
- **Target Areas:** `pyproject.toml`, `.env.example`, `src/`, `tests/`, `scripts/`, `.github/`

## Objective

Create the runnable Python project skeleton and shared developer tooling baseline the rest of the tasks will build on.

## Deliverables

- root Python project with `src/egyptian_voice_agent/`
- `pyproject.toml` using `uv`
- starter package directories and `__init__.py` files
- starter `tests/` layout
- starter lint, type-check, and test commands

## Acceptance Criteria

- [ ] `pyproject.toml` exists and installs the project in editable form
- [ ] the target source tree exists under `src/egyptian_voice_agent/`
- [ ] `.env.example` documents all currently known environment variables
- [ ] `tests/` and `scripts/` exist with placeholders or bootstrap helpers
- [ ] baseline commands for linting, typing, and tests are documented and runnable where applicable

## Notes

- This task owns repository structure, not business logic.
